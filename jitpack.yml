jdk:
  - openjdk17
before_install:
  - echo "Velra — io.github.u-nyxx:velra — via JitPack"
install:
  - ./gradlew :velra:assembleRelease
  - ./gradlew :velra:testDebugUnitTest
before_cache:
  - rm -rf ~/.gradle/caches/modules-2/files-2.1